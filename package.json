{
  "name": "sauce-demo",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "npx playwright test",
    "test:login": "npx playwright test --grep @login",
    "test:inventory-filter": "npx playwright test --grep @inventory-filter",
    "test:filter": "npx playwright test --grep @filter",
    "test:positive": "npx playwright test --grep @positive",
    "test:negative": "npx playwright test --grep @negative",
    "test:headed": "npx playwright test --headed",
    "test:debug": "npx playwright test --debug",
    "allure:generate": "allure generate allure-results --clean",
    "allure:open": "allure open",
    "allure": "allure generate allure-results --clean && allure open",
    "clean": "rimraf allure-results allure-report test-results playwright-report",
    "test:clean": "npm run clean && playwright test"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.61.0",
    "@types/node": "^26.0.0",
    "allure-commandline": "^2.43.0",
    "allure-playwright": "^3.10.2",
    "rimraf": "^6.1.3"
  },
  "dependencies": {
    "dotenv": "^17.4.2"
  }
}
